types:
  PlayerName: string
  EntityType:
    - SHIP
    - CANNON_BALL
  Point:
    x: number
    y: number
  Entity:
    id: string
    type: EntityType
    location: Point
    angle: number
  PlayerState:
    entities: Entity[]
    updatedAt: number

methods:
  createGame:
  moveShip:
    target: Point
  fireCannon:
    target: Point

auth:
  anonymous:
    separator: "-"

userState: PlayerState
initialize: createGame
error: string
tick: true
