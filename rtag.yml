types:
  PlayerName: string
  EntityType:
    - SHIP
  Point:
    x: number
    y: number
  Entity:
    id: string
    type: EntityType
    location: Point
  PlayerState:
    entities: Entity[]
    target?: Point
    updatedAt: number

methods:
  createGame:
  updateTarget:
    target: Point

auth:
  anonymous:
    separator: "-"

userState: PlayerState
initialize: createGame
error: string
tick: true
